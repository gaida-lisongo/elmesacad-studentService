/**
 * URL publique de base du service étudiant (QR code, pied de page PDF).
 * Suffixe typique : `/api/commandes/verify/:orderId`.
 */
export const verificationBase = (
  'https://services.inbtp.ac.cd/student'
);
